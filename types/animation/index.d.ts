import type { Animate } from "../../../dative";

export declare function flip({ animate, duration, delay }: Animate): void;

export declare function fadeOut({ animate, duration, delay }: Animate): void;

export declare function bubble({ animate, duration, delay }: Animate): void;

export declare function swing({ animate, duration, delay }: Animate): void;
